% [GammaCount] = getgamma;   
%
% Number of counts from the radiation monitor in Sector 11
%


    %count1 = getGamma;
    %temp = getGamma;
    %Time0=clock;
    %while (temp == count1)
    %  temp = getGamma;
    %  DeltaTime = etime(clock,Time0);
    %  if (DeltaTime > 10), disp('Time-out on gamma() (10 seconds)'); break; end;
    %end;
    %count2 = temp;
    %temp = getGamma;
    %Time0=clock;
    %while (temp == count2)
    %  temp = getGamma;
    %  DeltaTime = etime(clock,Time0);
    %  if (DeltaTime > 10), disp('Time-out on gamma() (10 seconds)'); break; end;
    %end;
    %count3 = temp;
    %count(i) = (count1 + count2 + count3)/3;
    %count(i) = (count1 + count2)/2;
